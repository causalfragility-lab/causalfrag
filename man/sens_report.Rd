% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stubs.R
\name{sens_report}
\alias{sens_report}
\title{Run the full sensitivity analysis pipeline}
\usage{
sens_report(
  model,
  treatment,
  data,
  design = NULL,
  narrative = "standard",
  plot = TRUE,
  ...
)
}
\arguments{
\item{model}{A fitted model object (e.g. from `lm()`, `glm()`).}

\item{treatment}{Character. Name of the treatment variable in the model.}

\item{data}{A data frame containing the variables in `model`.}

\item{design}{Character. Override automatic design detection. One of
`"regression"`, `"matched"`, `"iv"`, `"survival"`. Default `NULL`
(auto-detect).}

\item{narrative}{Character. Level of narrative detail. One of `"brief"`,
`"standard"`, `"reviewer-ready"`. Default `"standard"`.}

\item{plot}{Logical. Produce sensitivity plots. Default `TRUE`.}

\item{...}{Additional arguments passed to `run_sensitivity()`.}
}
\value{
A `sens_results` object with all fields populated.
}
\description{
The main user-facing function. Runs the complete pipeline:
detect design → run sensitivity → flag fragility → interpret → visualize.
}
\examples{
\dontrun{
fit <- lm(mpg ~ am + wt + hp, data = mtcars)
result <- sens_report(fit, treatment = "am", data = mtcars)
print(result)
cat(result$narrative)
}

}
